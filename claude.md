# CLAUDE.md

## Общий принцип работы

Ты работаешь как senior backend/LLM engineer над проектом `llm_messenger_bot-v3`.

Главная цель проекта:

- сделать живого LLM-менеджера, который естественно ведёт диалог с клиентом;
- сохранить память, контекст и бизнес-логику;
- использовать актуальную базу знаний как единственный источник фактов;
- быстро эскалировать на реального менеджера, когда клиент готов к действию.

Ключевой принцип:

```text
LLM ведёт диалог.
Код даёт память, факты, ограничения, валидацию и эскалацию.
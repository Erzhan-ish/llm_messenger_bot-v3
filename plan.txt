Ты работаешь с llm_messenger_bot-v3.

Архитектура conversation_brain стала лучше: код больше не должен жёстко классифицировать сценарии. Но сейчас brain теряет контекст follow-up сообщений и иногда отвечает слишком общо.

Цель:
Сохранить формулу:
LLM думает и пишет живой текст.
Код хранит память, даёт чистые facts/action contracts, проверяет и выполняет.

Не возвращать старый regex-routing.
Нужно усилить memory + answer_contract.

============================================================
1. Не повторять intro
============================================================

Файлы:
app/services/conversation_brain.py
app/services/context_builder.py
app/processing/message_processor.py

Проблема:
Бот повторяет “Здравствуйте! Я Алексей...” в следующих ответах.

Сделать:
- В memory передавать `_introduced`.
- После первого intro сохранять `_introduced=True`.
- В prompt brain добавить:

Если memory._introduced=true:
- не начинай ответ с “Здравствуйте”;
- не представляйся снова;
- сразу отвечай по сути.

Acceptance:
Первый ответ может быть с представлением.
Второй и следующие ответы не начинаются с “Здравствуйте, я Алексей”.

============================================================
2. Сохранять pending question после “ЮЛ или ФЛ?”
============================================================

Файлы:
app/services/conversation_brain.py
app/processing/message_processor.py
app/services/context_builder.py

Проблема:
После вопроса “ЮЛ или ФЛ?” клиент отвечает “ФЛ”, но brain не продолжает подбор для ФЛ.

Когда brain спрашивает:
“Счёт подбираем для юрлица или физлица?”
или
“Для начала определим тип: ЮЛ или ФЛ?”

state_update должен сохранять:

{
  "active_task": {
    "type": "bank_selection",
    "pending_question": "client_type",
    "source": "partner_banks"
  },
  "last_topic": "partner_banks"
}

Если пользователь отвечает:
“ФЛ” / “физ лицо” / “физик”

context_builder должен положить:
current_entities.client_type = "ФЛ"

Brain должен использовать active_task и ответить по ФЛ, а не повторять список банков.

Expected:
“Для физлица основной рабочий вариант — ТКБ: открытие 1500 руб., ведение бесплатно. Расходные операции лучше заранее согласовывать с юристами банка. Смотрим тарифы или документы?”

Acceptance:
User: “ФЛ”
Bot: отвечает вариант для ФЛ, не общий список банков.

============================================================
3. Обработать короткое “какие?” через memory
============================================================

Файлы:
app/services/conversation_brain.py
app/services/context_builder.py

Проблема:
User: “какие?”
Bot повторяет список партнёрских банков.

Если user_text короткий:
- “какие?”
- “какие именно?”
- “что есть?”
- “какой вариант?”

И memory.active_task или memory.last_topic есть, brain обязан интерпретировать это как продолжение прошлой темы.

Пример memory:
{
  "last_topic": "partner_banks",
  "client_type": "ФЛ"
}

User:
“какие?”

Expected:
“Для физлица сейчас основной вариант — ТКБ: открытие 1500 руб., ведение 0 руб. в месяц. Хотите, подскажу документы?”

Prompt:
Короткие вопросы вроде “какие?”, “а что есть?”, “дальше?” всегда трактуй через recent_dialog и active_task. Не начинай новую тему.

Acceptance:
“какие?” после “ФЛ” не повторяет партнёрские банки, а раскрывает варианты для ФЛ.

============================================================
4. Добавить answer_contract в fact_pack
============================================================

Файлы:
app/services/context_builder.py
app/services/fact_retriever.py

Нужен не routing, а подсказка формата ответа.

Добавить в context:

answer_contract = {
  "must_answer_current_question": true,
  "do_not_repeat_intro": true,
  "do_not_repeat_previous_answer": true,
  "max_questions": 1,
  "style": "natural_short"
}

Для partner_banks:
answer_contract = {
  "topic": "partner_banks",
  "must_include": ["Альфа-Банк", "ТКБ", "Уралсиб"],
  "should_include": ["Т-Банк, МКБ и Росбанк на паузе"],
  "do_not_include": ["opening_fee", "monthly_fee"],
  "next_question": "Счёт подбираем для юрлица или физлица?"
}

Для FL bank selection:
answer_contract = {
  "topic": "bank_selection_fl",
  "must_include": ["ТКБ", "открытие 1500", "ведение 0"],
  "should_include": ["расходные операции согласуются юристами банка"],
  "do_not_include": ["Альфа как вариант для ФЛ без факта", "общий список банков"]
}

Для card:
answer_contract = {
  "topic": "debtor_card",
  "must_include": ["карта оформляется после введения реализации имущества", "подписывает финансовый управляющий"],
  "do_not_include": ["наличные", "судебное решение о выдаче наличными"],
  "next_question": "Реализация уже введена?"
}

Это НЕ старая классификация. Это контракт качества ответа для LLM.

============================================================
5. Исправить card answer contract
============================================================

Файлы:
app/services/context_builder.py
app/services/conversation_brain.py

Проблема:
Бот говорит “Нет проблем с картой”, хотя дальше ограничение.

Для вопроса про карту должника fact_pack.card_rules.primary должен быть:

“Если у гражданина введена реализация имущества, новая банковская карта оформляется и подписывается исключительно финансовым управляющим.”

Prompt:
Если клиент спрашивает “можно карту сделать?”:
- не говори “нет проблем”;
- не отвечай про наличные;
- объясни условие: после введения реализации имущества;
- спроси, введена ли реализация.

Expected:
“Карту можно оформить после введения реализации имущества: подписывает её финансовый управляющий. Реализация уже введена?”

Acceptance:
Нет фразы “нет проблем”.
Нет фразы про судебное решение о наличных.
Есть вопрос про реализацию имущества.

============================================================
6. Исправить follow-up “почему?”
============================================================

Файлы:
app/services/conversation_brain.py

Если last_topic или active_task связан с debtor_card / card_realization, а user_text:
- почему?
- а почему?
- почему нельзя?
- в чем причина?

Brain должен объяснить причину, а не повторять ответ.

Expected:
“Потому что после введения реализации имуществом должника распоряжается финансовый управляющий, поэтому карту оформляет и подписывает именно он. До этой стадии карту на должника не оформляем.”

Prompt:
Если клиент спрашивает “почему?” после ограничения, объясни причину простыми словами. Не повторяй прошлый ответ дословно.

Acceptance:
“почему?” после карты → explanation, no duplicate.

============================================================
7. Validator должен ловить wrong-topic answers
============================================================

Файл:
app/services/response_validator.py

Добавить проверки по answer_contract:

- Если contract.do_not_include содержит “наличные”, а reply содержит “наличные” или “судебное решение о выдаче наличными” → validation_error=wrong_topic_fact.
- Если contract.topic=partner_banks и reply содержит тарифы → validation_error=answered_tariffs_when_asked_bank_list.
- Если contract.topic=bank_selection_fl и reply не содержит “ТКБ” → validation_error=missing_primary_fact.
- Если memory._introduced=true и reply startswith “Здравствуйте” → validation_error=repeated_intro.
- Если user_text == “почему?” и reply near_duplicate previous → validation_error=did_not_explain_reason.

Repair prompt:
“Исправь ответ: ты использовал не тот факт / повторил приветствие / не ответил на follow-up. Используй answer_contract.”

Acceptance:
Ошибки не уходят пользователю, сначала repair.

============================================================
8. Улучшить state_update от conversation_brain
============================================================

Файл:
app/services/conversation_brain.py

Brain должен всегда возвращать state_update:

{
  "last_topic": "...",
  "active_task": {...},
  "client_type": "...",
  "pending_question": "...",
  "last_answer_summary": "..."
}

После partner_banks вопроса:
last_topic=partner_banks
pending_question=client_type

После ответа “ФЛ”:
client_type=ФЛ
last_topic=bank_selection_fl
active_task.type=bank_selection
active_task.client_type=ФЛ

После card question:
last_topic=debtor_card
pending_question=realization_status

После “почему?”:
last_topic=debtor_card
pending_question=realization_status сохраняется, если вопрос всё ещё актуален.

Acceptance:
В логах после каждого ответа виден state_update с last_topic и pending_question.

============================================================
9. Prompt: меньше канцелярита и скрипта
============================================================

Файл:
app/services/conversation_brain.py

Добавить style rules:

- Не начинай с “Понятно” без причины.
- Не пиши “Вы интересуетесь...” — отвечай прямо.
- Не повторяй формулировку клиента.
- Не говори “для начала давайте определим тип вашей организации”, лучше: “Счёт для юрлица или физлица?”
- Не говори “у нас есть варианты со сниженными тарифами”, если нет конкретики.
- Если знаешь конкретный вариант — назови его.
- Пиши 1–3 предложения.
- Один вопрос максимум.

Bad:
“Вы интересуетесь нашими партнерскими банками.”

Good:
“Сейчас активные варианты — Альфа-Банк, ТКБ и Уралсиб. Для физлица чаще смотрим ТКБ. Счёт для физлица?”

============================================================
10. Tests
============================================================

Добавить:

test_no_repeated_intro
Context introduced=true
Expected reply does not start with Здравствуйте.

test_partner_banks_then_fl
User: с какими банками?
Bot asks ЮЛ/ФЛ
User: ФЛ
Expected: TKB FL pricing, not partner list.

test_short_which_followup
After client_type=ФЛ
User: какие?
Expected: TKB option.

test_card_not_no_problem
User: карту сделать?
Expected: no “нет проблем”; asks realization.

test_card_why_explains_reason
After card answer
User: почему?
Expected: reason explanation, not duplicate.

test_validator_blocks_cash_when_card
answer_contract.topic=debtor_card
reply mentions cash withdrawal
Expected validation_error.

test_validator_blocks_repeated_intro
introduced=true
reply starts with Здравствуйте
Expected validation_error.

============================================================
FINAL ACCEPTANCE
============================================================

Готово, если:
1. После “ФЛ” бот говорит конкретно про ТКБ для ФЛ.
2. После “какие?” бот продолжает прошлую тему.
3. Бот не повторяет приветствие.
4. Карта не путается с наличными.
5. “Почему?” объясняет причину, а не повторяет.
6. Ответы звучат живо, без фраз “Вы интересуетесь...”.